import React from 'react'
import { View, StyleField } from 'react-native'
import Field from './Field'

export default props => {
    const rows = props.board.map((rows, r) => {
        const columns = row.map((field, c) => {
            
        })
    })
}